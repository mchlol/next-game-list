import ListDisplay from "./ListDisplay";
import ListDisplayTitle from "./ListDisplayTitle";
import ListDisplayCard from "./ListDisplayCard";

ListDisplay.Title = ListDisplayTitle;
ListDisplay.Card = ListDisplayCard;

export default ListDisplay;